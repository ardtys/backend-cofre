# ========================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ========================================
# This file contains recommended settings for production deployment
# Copy to .env on your production server and update all values

# ========================================
# APPLICATION - PRODUCTION SETTINGS
# ========================================
APP_NAME="Cofre API"
APP_ENV=production
APP_KEY=base64:GENERATE_NEW_KEY_WITH_php_artisan_key:generate
APP_DEBUG=false                           # ❗ CRITICAL: Must be false in production!
APP_TIMEZONE=Asia/Jakarta                 # Set to your timezone
APP_URL=https://api.cofre.app            # Your production domain

APP_LOCALE=en
APP_FALLBACK_LOCALE=en

# ========================================
# SECURITY
# ========================================
# Higher bcrypt rounds for production (more secure, slower)
BCRYPT_ROUNDS=12

# ========================================
# LOGGING - PRODUCTION
# ========================================
LOG_CHANNEL=stack
LOG_STACK=daily                           # Use daily rotation
LOG_LEVEL=error                           # Only log errors in production
LOG_DEPRECATIONS_CHANNEL=null

# ========================================
# DATABASE - PRODUCTION (MySQL/PostgreSQL)
# ========================================
# SQLite is NOT recommended for production!
# Use MySQL or PostgreSQL for better performance and scalability

# MySQL Configuration:
DB_CONNECTION=mysql
DB_HOST=your-db-host.com                  # RDS, managed DB, etc.
DB_PORT=3306
DB_DATABASE=cofre_production
DB_USERNAME=cofre_user
DB_PASSWORD=your_strong_secure_password_here

# PostgreSQL Alternative:
# DB_CONNECTION=pgsql
# DB_HOST=your-postgres-host.com
# DB_PORT=5432
# DB_DATABASE=cofre_production
# DB_USERNAME=cofre_user
# DB_PASSWORD=your_strong_secure_password_here

# ========================================
# SESSION & CACHE - PRODUCTION
# ========================================
SESSION_DRIVER=database                   # Or 'redis' for better performance
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=.cofre.app                 # Your domain
SESSION_SECURE_COOKIE=true                # Require HTTPS
SESSION_SAME_SITE=lax

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=s3                        # Use S3/R2 for file storage
QUEUE_CONNECTION=database                 # Or 'redis' for better performance

CACHE_STORE=redis                         # Redis recommended for production
CACHE_PREFIX=cofre_

# ========================================
# REDIS (Recommended for Production)
# ========================================
# Install Redis for better cache/queue performance
REDIS_CLIENT=phpredis
REDIS_HOST=your-redis-host.com
REDIS_PASSWORD=your_redis_password
REDIS_PORT=6379
REDIS_DB=0
REDIS_CACHE_DB=1

# ========================================
# EMAIL - PRODUCTION (Real SMTP)
# ========================================
# Gmail Example (Use App Password, not regular password):
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_specific_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@cofre.app
MAIL_FROM_NAME="${APP_NAME}"

# Or use dedicated email service:
# - SendGrid: smtp.sendgrid.net
# - Mailgun: smtp.mailgun.org
# - Amazon SES: email-smtp.region.amazonaws.com

# ========================================
# AWS S3 / CLOUDFLARE R2 - PRODUCTION
# ========================================
# Use AWS IAM roles if deploying on EC2 (more secure)
# Or provide credentials below
AWS_ACCESS_KEY_ID=your_production_access_key
AWS_SECRET_ACCESS_KEY=your_production_secret_key
AWS_BUCKET=cofre-production-videos
AWS_ENDPOINT=https://your-account.r2.cloudflarestorage.com
AWS_URL=https://cdn.cofre.app              # Your CDN URL
AWS_REGION=auto
AWS_USE_PATH_STYLE_ENDPOINT=true

# ========================================
# GOOGLE GEMINI AI - PRODUCTION
# ========================================
# Use separate API key for production
# Set up usage limits and monitoring
GEMINI_API_KEY=your_production_gemini_key

# ========================================
# VIDEO MODERATION - PRODUCTION
# ========================================
# IMPORTANT: Disable auto-approval in production!
VIDEO_AUTO_APPROVE=false                   # ❗ Require manual moderation

# ========================================
# CORS (Cross-Origin Resource Sharing)
# ========================================
# Configure allowed origins for your frontend
SANCTUM_STATEFUL_DOMAINS=cofre.app,www.cofre.app
SESSION_DOMAIN=.cofre.app

# ========================================
# RATE LIMITING
# ========================================
# These are set in routes/api.php
# Review and adjust based on your needs

# ========================================
# SSL/HTTPS
# ========================================
# Force HTTPS (handled by web server config)
# Nginx/Apache should redirect HTTP to HTTPS
# Set up SSL certificate (Let's Encrypt recommended)

# ========================================
# MONITORING & ERROR TRACKING
# ========================================
# Optional: Sentry for error tracking
# SENTRY_LARAVEL_DSN=https://your-sentry-dsn
# SENTRY_TRACES_SAMPLE_RATE=0.2

# ========================================
# PERFORMANCE OPTIMIZATION
# ========================================
# Before deploying, run these commands:
# php artisan config:cache
# php artisan route:cache
# php artisan view:cache
# php artisan optimize

# ========================================
# BACKUP CONFIGURATION
# ========================================
# Set up automated database backups
# Daily backups to S3/R2 recommended

# ========================================
# DEPLOYMENT CHECKLIST
# ========================================
# [ ] APP_DEBUG=false
# [ ] APP_ENV=production
# [ ] Strong APP_KEY generated
# [ ] Real database configured (MySQL/PostgreSQL)
# [ ] Redis installed and configured
# [ ] SMTP email configured and tested
# [ ] SSL certificate installed
# [ ] HTTPS enforced
# [ ] New S3/R2 credentials (rotate from dev)
# [ ] New Gemini API key (rotate from dev)
# [ ] VIDEO_AUTO_APPROVE=false
# [ ] CORS domains configured
# [ ] Rate limits reviewed
# [ ] Cron jobs configured for queue:work
# [ ] Backups configured
# [ ] Monitoring/logging setup
# [ ] Load tested
# [ ] Security audit completed

# ========================================
# POST-DEPLOYMENT COMMANDS
# ========================================
# On production server, run:
# php artisan migrate --force
# php artisan db:seed --class=AdminSeeder
# php artisan storage:link
# php artisan config:cache
# php artisan route:cache
# php artisan view:cache
# php artisan optimize

# Restart queue workers:
# php artisan queue:restart

# For zero-downtime deployments, use:
# php artisan down
# git pull
# composer install --optimize-autoloader --no-dev
# php artisan migrate --force
# php artisan cache:clear
# php artisan config:cache
# php artisan route:cache
# php artisan view:cache
# php artisan queue:restart
# php artisan up
